{% extends 'base.html.twig' %}

{% block body %}

	{{ form(formular) }}
	
	{{ form_start(formular) }}
	
	    {{ form_row(formular.username) }}

		{{ form_row(formular.firstname) }}
		
    	{{ form_row(formular.lastname) }}
    	
    	{{ form_row(formular.email) }}
    	
    	{{ form_row(formular.password) }}
    	
	{{ form_end(formular) }}
	
{% endblock %}